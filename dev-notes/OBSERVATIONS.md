# Observations — kaemmung

Grounded incidents and techniques from real use. Every SKILL.md
rule cites its incident here (fire-born discipline, CLAUDE.md).

## 2026-08-11 — founding incidents (three, across two estates)

Full detail: PLAN.md §founding evidence. Short form:

1. cache-fix BACKLOG.md, 38 → 14,396 lines in 13 days — diseases
   capture-dominance, grade inflation (95 READY, nobody believed
   the label), unresolved boundaries (63/100 READY named no file).
   Rebuilt same day with third grade + booking-bar lint + Done
   home. Grounds SKILL.md diseases 2, 3, 4.
2. pbs-office FEATURE-BACKLOG.md, 578 → 2,769 lines in 25 days,
   ~98 closure markers vs ~47 live entries in live sections — the
   carrier's own "never delete silently" rule lived as
   strike-and-keep, forbidding the exit. Grounds disease 1 and the
   "the rule is the defect, never the entries" clause.
3. Both carriers crossed the corpus's prose tripwire with nothing
   firing ("an unread ratio fires nothing") — grounds the
   measure-first section's banner-as-intake clause; the computed
   halves live in dotfiles session-scan.py (retirement_pressure,
   exit_pressure), each red-proven on its motivating case, each
   first-build REFUTED by that red probe (a 14-day window diluted
   the ratio; a closed-vs-live comparison was inflated by index
   bullets) — evidence for "measure before touching" applying to
   the instruments themselves.

Every clause of SKILL.md v0.1.0 traces to these incidents; the
naming deviation (kaemmung over the canonical "grooming") is
argued in PLAN.md and open until the cold probe (BACKLOG).

## 2026-08-11 — birth review (skill-craft review-checklist pass, condensed)

Consumer declared (top-tier, dispatch-default for the pass);
evidence register throughout; scope cue ("Not for …") in the
description; one meaning one home (carrier-design rules cited to
the operator corpus, never restated; diseases co-located with
markers and cures); completion criteria checkable (reconciliation
sum, negative grep with liveness). Recorded deferrals: (1)
mechanical frontmatter validation not run — schema copied from
begehung's working 0.1.2 pattern; run before first release past
trial. (2) Tier-1/Tier-2 sets written but unexecuted (BACKLOG,
READY) — the description's fire rate is unmeasured until then.
(3) Naming open by design: the PLAN argues kaemmung over the
canonical "grooming" (wrong priors: scrum refinement); the cold
probe decides, per skill-craft's surface-the-substitution rule.
