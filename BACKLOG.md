# Backlog — kaemmung

## Open

- **READY — Tier-1 eval run.** Query set operator-blessed in
  PLAN.md §Evaluation; runner /eval-skill after plugin install.
  Verifier: fire-rate table in dev-notes/eval-kaemmung/<date>/.
  Done-criterion: no should-trigger miss 3/3, no over-trigger.
  Write boundary: this repo.
- **READY — Trial run 1: the pbs-office Kämmung.** The queued
  pbs-office brief (exit repair + retirement pass + bundling table
  + re-measure) IS the trial: once the operator relays it and the
  pass runs, grade the execution against PLAN.md §Tier-2 signature
  and log the observation. Verifier: OBSERVATIONS.md entry with
  per-signature-point grades. Done-criterion: entry written; any
  fire-born gap booked. Write boundary: this repo (the pass itself
  is the executing session's, not ours).
- **PARKED — Three-arm Tier-2 (with/without/sentence).** Missing
  evidence: a second real carrier case to run it on without
  contaminating trial run 1 (the pbs-office pass will already have
  run skill-guided). Trigger: next diseased carrier surfacing after
  trial 1.

## Done

- **DONE — Name cold-probe: kaemmung vs grooming** (2026-08-11).
  Both arms run (2 × sonnet, name + situation only); verdict KEEP
  kaemmung, argued basis corrected by measurement — grooming did
  not misfire as feared. Evidence:
  dev-notes/eval-kaemmung/2026-08-11/ (result.md + both
  transcripts); PLAN.md naming section updated.
